import InternshipApplicationPage from "@/pages/student/InternshipApplicationPage";

// Re-export the same page component — it already handles both student and coordinator views internally
export default InternshipApplicationPage;
